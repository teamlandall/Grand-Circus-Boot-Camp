var daysOfWeek = ["monday", "tuesday", "wednesday", "thursday", "friday"];


var firstThreeLetters = function(element, index){
	if (index % 2 !== 0) {
	console.log(element);
	}
};

daysOfWeek.forEach(firstThreeLetters);